using namespace std;
#include <iostream>
#include <string>
bool ValidatePassword(const string& password)
{
   
    if (password.length() < 8) {
        return false;
    }

         ':', '!', '@', '#', '$', '%', '&', '*', '+', '?'
        for (char c : password)
        {
            if (!((c >= 'A' && c <= 'Z') || (c >= 'a' && c <= 'z') ||
                (c == ':' || c == '!' || c == '@' || c == '#' || c == '$' ||
                    c == '%' || c == '&' || c == '*' || c == '+' || c == '?'))) {
                return false;
            }
        }

    return true;
}



    int main() {
        setlocale(0, "");
        string password;
        cout << "Введите пароль: ";
        cin >> password;

        if (ValidatePassword(password)) {
            cout << "Пароль допустим." << std::endl;
        }
        else {
            cout << "Пароль недопустим." << std::endl;
        }

        return 0;
    }
